diff --git a/tests/research/test_tier2_calibrated_candidate.py b/tests/research/test_tier2_calibrated_candidate.py
index b03de20..0a0a3f3 100644
--- a/tests/research/test_tier2_calibrated_candidate.py
+++ b/tests/research/test_tier2_calibrated_candidate.py
@@ -19,6 +19,7 @@ from grid_trade.calibration.microstructure_contracts import (
 )
 from grid_trade.calibration.microstructure_engine import MicrostructureCalibrationConfig
 from grid_trade.calibration.order_flow import OfiImpactConfig
+from grid_trade.calibration.sampling import SamplingSpec
 from grid_trade.calibration.trend import TrendCalibrationConfig
 from grid_trade.calibration.universal_engine import UniversalCalibrationConfig
 from grid_trade.calibration.volatility import RobustVolatilityConfig
@@ -38,6 +39,7 @@ from grid_trade.datasets.contracts import (
     SourceFamily,
 )
 from grid_trade.datasets.manifest import DatasetManifest
+from grid_trade.domain.instrument import ContractType, InstrumentSpec
 from grid_trade.domain.risk import RiskLimits, RiskState
 from grid_trade.research.hftbacktest_adapter import HftReplayConfig
 from grid_trade.research.replay_attribution import MarketImpactEligibilityConfig
@@ -161,6 +163,14 @@ def _universal_config() -> UniversalCalibrationConfig:
             volatility=RobustVolatilityConfig(4, 2, robust_scale),
             trend=TrendCalibrationConfig(2, Decimal("1"), Decimal("0.000001"), Decimal("5")),
             funding=FundingCalibrationConfig(4, 2, robust_scale, Decimal("3")),
+            sampling=SamplingSpec(
+                observation_interval_ms=60_000,
+                interval_tolerance_ms=0,
+                volatility_window_ms=240_000,
+                trend_horizon_ms=120_000,
+                markout_horizon_ms=120_000,
+                ofi_horizon_ms=120_000,
+            ),
         ),
         microstructure=MicrostructureCalibrationConfig(
             intensity=IntensityCalibrationConfig(
@@ -217,7 +227,24 @@ def _meta() -> CalibratedAdaptiveMetaConfig:
     )
 
 
-def _config() -> Tier2CalibratedCandidateConfig:
+def _instrument(instrument_id: str = "BTC") -> InstrumentSpec:
+    return InstrumentSpec(
+        instrument_id=instrument_id,
+        contract_type=ContractType.LINEAR_PERPETUAL,
+        contract_multiplier=Decimal("1"),
+        tick_size=Decimal("0.01"),
+        quantity_step=Decimal("0.001"),
+        min_quantity=Decimal("0.001"),
+        min_notional=Decimal("0.01"),
+        max_quantity=Decimal("10"),
+        funding_interval_seconds=3_600,
+    )
+
+
+def _config(
+    instrument: InstrumentSpec | None = None,
+) -> Tier2CalibratedCandidateConfig:
+    resolved_instrument = instrument or _instrument()
     return Tier2CalibratedCandidateConfig(
         universal=_universal_config(),
         adaptive_meta=_meta(),
@@ -226,13 +253,11 @@ def _config() -> Tier2CalibratedCandidateConfig:
             max_single_move_loss_fraction=Decimal("0.01"),
             volatility_floor=Decimal("0.0001"),
         ),
-        venue=VenueGridConstraints(
-            tick_size=Decimal("0.01"),
-            quantity_step=Decimal("0.001"),
-        ),
+        venue=VenueGridConstraints.from_instrument(resolved_instrument),
         maker_fee_rate=Decimal("0.0001"),
         max_margin_notional=Decimal("100"),
         venue_max_quantity=Decimal("10"),
+        instrument=resolved_instrument,
     )
 
 
@@ -323,8 +348,25 @@ def test_calibrated_candidate_is_derived_from_causal_canonical_history() -> None:
     assert result.capacity.q_max > 0
     assert len(result.provenance_digest) == 64
     assert all(order.price % Decimal("0.01") == 0 for order in result.candidate_orders)
+    assert all(order.instrument_id == "BTC" for order in result.candidate_orders)
+    assert all(order.client_order_id.startswith("BTC:") for order in result.candidate_orders)
+
+
+def test_candidate_rejects_instrument_spec_mismatch() -> None:
+    events = _events()
 
+    with pytest.raises(ValueError, match="dataset/spec instrument mismatch"):
+        derive_tier2_calibrated_candidate(
+            dataset=_manifest(events),
+            events=events,
+            evidence_frames=_frames(),
+            decision_exchange_ts_ns=_ns(_time(13)),
+            config=_config(_instrument("ETH")),
+            equity=Decimal("100"),
+            starting_position=Decimal(0),
+        )
+
 def test_future_evidence_frame_cannot_change_earlier_candidate() -> None:
     events = _events()
     decision_timestamp_ns = _ns(_time(13))
